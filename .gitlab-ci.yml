image: kaiwinter/docker-java8-maven

java:
  script:
    - call mvn clean compile package
  artifacts:
    paths:
      - target/*.jar
  tags:
    - windows
java:
  script:
    - eval mvn clean compile package
  artifacts:
    paths:
      - target/*.jar
  tags:
    - linux